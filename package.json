{
  "name": "krmx",
  "version": "0.2.0",
  "description": "A TypeScript-based multi-client message sharing protocol build on top of WebSockets",
  "scripts": {
    "precommit": "npm run --prefix server precommit && npm run --prefix client precommit && npm run --prefix docs precommit"
  },
  "author": "Simon Karman",
  "license": "ISC",
  "husky": {
    "hooks": {
      "pre-commit": "npm run precommit"
    }
  },
  "dependencies": {
    "husky": "^4.3.8"
  }
}

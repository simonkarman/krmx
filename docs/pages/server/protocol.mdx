# Protocol
TODO

A connection made to the server needs to link to a user first. Once linked, a user can interact with the server. It does so by receiving realtime information about all users connected to the server and by being able to send (custom) messages to the server.

## Krmx Messages
... list all messages ...

## Custom Messages
```
{ type: string, payload: unknown }
```

